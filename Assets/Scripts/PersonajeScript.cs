using UnityEngine;

public class PersonajeScript : MonoBehaviour
{
    string emocion = "neutro";
    float exito = 1;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
